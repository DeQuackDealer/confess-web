services:
  rileyscrush:
    build: .
    ports:
      - '4000:4000'
    environment:
      - NODE_ENV=production
      - PORT=4000
    volumes:
      # Bind-mount /data so crushes.json / config.json persist across container rebuilds.
      - ./data:/app/data
    restart: unless-stopped
